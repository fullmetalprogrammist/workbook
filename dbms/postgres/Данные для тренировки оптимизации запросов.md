```sql
DROP TABLE orders CASCADE;
DROP TABLE users CASCADE;
```



```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE orders (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    amount NUMERIC(10,2) NOT NULL CHECK (amount > 0),
    status VARCHAR(20) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```



```sql
-- 2. Заполнение users (100 000 строк)
INSERT INTO users (id, name, created_at)
SELECT i, 'User_' || i, NOW() - (random() * INTERVAL '365 days')
FROM generate_series(1, 100_000) i;

-- 3. Заполнение orders случайными данными (20 млн строк)
INSERT INTO orders (id, user_id, amount, status, created_at)
SELECT i,
       (random() * 99999 + 1)::int,
       (random() * 9999 + 1)::numeric(10,2),
       'paid',
       NOW() - (random() * INTERVAL '365 days')
FROM generate_series(1, 20_000_000) as i;

-- 4. Добавление 50 000 заказов для user_id = 1
INSERT INTO orders (id, user_id, amount, status, created_at)
SELECT i, 1, (random() * 9999 + 1)::numeric(10,2), 'paid', NOW() - (random() * INTERVAL '30 days')
FROM generate_series(20_000_001, 20_050_000) i;

-- 5. Добавление 5000 заказов для user_id = 2
INSERT INTO orders (id, user_id, amount, status, created_at)
SELECT i, 2, (random() * 9999 + 1)::numeric(10,2), 'paid', NOW() - (random() * INTERVAL '30 days')
FROM generate_series(20_060_001, 20_065_001) i;

-- 6. Добавление 5000 заказов для user_id = 3
INSERT INTO orders (id, user_id, amount, status, created_at)
SELECT i, 3, (random() * 9999 + 1)::numeric(10,2), 'paid', NOW() - (random() * INTERVAL '30 days')
FROM generate_series(20_070_001, 20_075_001) i;
```

