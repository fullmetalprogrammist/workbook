

# На знание стандартной библиотеки

## Уникальные числа в массиве

Напишите несколько функций, которые принимают массив чисел и возвращают новый массив, содержащий только уникальные элементы (элементы, встречающиеся ровно один раз).

Решение должно быть реализовано различными способами:

- С использованием Map: создаем карту для подсчета вхождений каждого элемента. Фильтруем исходный массив, оставляя только элементы, встречающиеся один раз.
- Добавление метода в прототип Array: аналогичное решение, но реализованное как метод для массива.
- С использованием метода filter и indexOf: оставляем элемент, если его первое вхождение совпадает с последним (то есть, он уникален).
- С использованием reduce: формируем объект-счетчик, затем фильтруем массив по значению счетчика.
- С использованием forEach: используем два набора (Set) для хранения уникальных элементов и дубликатов, а затем фильтруем исходный массив по найденным уникальным значениям.

**Решение**

 ```javascript
// Через Map
function filterUnique(arr) {
  const freqs = new Map();

  arr.forEach(x => freqs.set(x, (freqs.get(x) || 0) + 1));

  return arr.filter(x => freqs.get(x) === 1);
}


const arr = [7, 3, 7, 5, 8, 4, 4];
const uniqueNumbers = filterUnique(arr);

console.log(uniqueNumbers);  // 3, 5, 8
 ```











